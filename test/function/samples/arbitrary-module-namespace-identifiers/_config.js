module.exports = defineTest({
	// TODO Lukas while this works in theory, code positions seem to be off completely if emojis are used
	skip: true,
	description: 'supports arbitrary module namespace identifiers'
});

import externalUrlChangedOld from 'https://external-url.com/changed-old.js';
import externalUrlUnchanged from 'https://external-url.com/unchanged.js';
import externalChangedOld from 'external-changed-old';
import externalUnchanged from 'external-unchanged';
import externalRelativeChangedOld from '../external-relative-changed-old';
import externalRelativeUnchanged from '../external-relative-unchanged';
import internalChangedOld from './internal-changed-old';
import internalUnchanged from './internal-unchanged';

export {
    externalUrlChangedOld,
    externalUrlUnchanged,
    externalChangedOld,
    externalUnchanged,
    externalRelativeChangedOld,
    externalRelativeUnchanged,
    internalChangedOld,
    internalUnchanged
};

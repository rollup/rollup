export function add1(a, b, enable) {
	if (enable) {
        return a + b;
    }
    return a - b;
}

export function add2(a, b, enable) {
	if (enable) {
        return a + b;
    }
    return a - b;
}
